import { type Config } from 'prettier';

const config: Config = {
  arrowParens: 'avoid',
  bracketSpacing: true,
  printWidth: 120,
  semi: true,
  singleQuote: true,
  trailingComma: 'none',
  useTabs: false
};

export default config;
